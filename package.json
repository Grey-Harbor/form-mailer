{
  "name": "form-mailer",
  "version": "0.1.0",
  "type": "module",
  "description": "Lightweight form-to-email delivery for Node.js apps and serverless handlers.",
  "repository": {
    "type": "git",
    "url": "git@github.com:cuzz22000/form-mailer.git"
  },
  "bugs": {
    "url": "https://github.com/cuzz22000/form-mailer/issues"
  },
  "homepage": "https://github.com/cuzz22000/form-mailer#readme",
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist/src",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "ci": "npm run check && npm test",
    "pack:check": "npm pack --dry-run",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "keywords": [
    "smtp",
    "email",
    "forms",
    "nodejs",
    "typescript"
  ],
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    },
    "./package.json": "./package.json"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3"
  }
}
